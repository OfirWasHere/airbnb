// add new airbnb house to the list
// If created new listing then rating = 1;
// Post http://localhost:4000/api/list/add


// delete delete existing airbnb house if user owns it
// Delete http://localhost:4000/api/list/:id

// update existing airbnb house if user owns it
// Put http://localhost:4000/api/

